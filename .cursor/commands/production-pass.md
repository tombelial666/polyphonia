Read `MANIFEST.md`, `STRUCTURE.md`, and `docs/dev-workflow/commands-reference.md`.

Then:

1. use `producer` as the primary specialist
2. classify the request as arrangement, sound design, recording, or mix direction
3. if an instrument is central, use the matching reusable instrument skill
4. return the top priority passes in order with actionable tradeoffs
5. end with one concrete next studio move
