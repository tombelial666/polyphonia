Read `MANIFEST.md`, `STRUCTURE.md`, `docs/architecture.md`, and `docs/dev-workflow/commands-reference.md`.

Then:

1. classify the request as composition, theory, production, solfege, or mixed
2. route the work through `music-director` as the primary framing role
3. if an instrument is central, apply the matching reusable instrument skill
4. separate key points into `Confirmed`, `Assumption`, `Risk`, and `Open Question` where useful
5. return one integrated, reviewable recommendation rather than a dump of disconnected advice
