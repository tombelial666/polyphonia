When a change affects project direction, workflow, file-authority boundaries, or desktop-shell roadmap:

1. update `MANIFEST.md` if direction or scope changed
2. update affected files in `docs/`
3. update `STRUCTURE.md` if boundaries changed
4. keep `AGENTS.md` and `CLAUDE.md` synchronized
5. do not leave the decision only in chat
