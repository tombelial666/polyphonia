Use this command when evaluating or planning PETS work that touches runtime shell decisions.

Rules:

1. describe the current `pywebview` path first
2. separate current implementation from `Electron` or `Tauri` roadmap ideas
3. prefer boundary cleanup and clearer source files before shell replacement
4. record risks and trade-offs explicitly
