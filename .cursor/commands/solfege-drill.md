Read `MANIFEST.md`, `STRUCTURE.md`, and `docs/dev-workflow/commands-reference.md`.

Then:

1. use `solfege-teacher` as the primary specialist
2. identify the learner level and one target skill only
3. if an instrument is central, use the matching reusable instrument skill
4. create a short graded exercise with answer key
5. end with the most likely mistake and the next drill
