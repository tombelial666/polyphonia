# QA package — offline scales / fretboard / chords

Документы для этой фичи лежат **рядом** с автотестами `tests/e2e/test_offline_scales_chords_e2e.py`, но не смешиваются с ними: здесь только план, трассировка и runbook.

**Машинные отчёты** (JUnit и т.п.) пишутся только в `qa/results/` — см. `test-execution-runbook.md`.
