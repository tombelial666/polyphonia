Read `MANIFEST.md`, `STRUCTURE.md`, `docs/architecture.md`, and any relevant file under `docs/`.

Then:

1. identify the source-of-truth files for the requested work
2. classify key points into `Confirmed`, `Assumption`, `Risk`, `Deferred`, and `Open Question` where useful
3. if the task is substantial, create or update a task file under `tasks/`
4. prefer the smallest safe plan before editing
