Read `MANIFEST.md`, `STRUCTURE.md`, and `docs/dev-workflow/commands-reference.md`.

Then:

1. use `composer` as the primary specialist
2. identify tonal center, style, mood, tempo, and instrument constraints
3. if an instrument is central, use the matching reusable instrument skill
4. return 2-3 contrasted composition options with explicit reasoning
5. end with one recommendation, one risk, and one revision lever
